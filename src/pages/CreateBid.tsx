import { FC } from "react";
import NewBid from "../components/NewBid/NewBid";
// import withAuth from "../hoc/withAuth";

const CreateBid: FC = () => {
  return <NewBid />;
};

export default CreateBid;

//export default withAuth(CreateBid);
